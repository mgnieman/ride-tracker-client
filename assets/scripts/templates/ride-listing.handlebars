{{#each rides as |ride|}}

  <!-- {{> partial}}
  Don't uncomment yet -->

 <!-- Developers, why are we using data-id and not just id? -->
  <h1 data-id="{{ride.id}}"> <small>Date: {{ride.date}}</small></h1>

{{/each}}
